# adhoc

One-off, single-advertisement and smoke-test scripts, moved out of
`scripts/` during the Final Production Lock (§22 cold storage).

Nothing here is on the live call graph; no live code, test or npm script
references it. Historical record only — see `../README.md` for the rule.
